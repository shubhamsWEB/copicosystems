/** @type {import('next').NextConfig} */
const nextConfig = {
    // reactStrictMode: true,
  output: 'export',
// assetPrefix: './',
  trailingSlash: true,
  reactStrictMode: true
}

module.exports = nextConfig
